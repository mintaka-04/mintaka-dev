---
title: "2026-05-02 TIL"
date: 2026-05-02
draft: false
categories: ["til"]
tags: ["ec2", "cd", "ssm", "python"]
---

## 오늘 할 일
- Python 워커 EC2에 배포
- GitHub Actions로 CD 파이프라인 구성

## 한 일
- Ubuntu 24.04 t3.micro EC2 인스턴스 생성
- systemd로 워커 서비스 등록
- GitHub Actions CD 파이프라인 구성 (SSH → SSM으로 전환)
- EC2 보안 그룹 포트 22 차단

## 배운 것
- aws ec2에서 ssh로 터미널 연결 및 ssm으로 전환해서 브라우저에서 로그 볼 수 있었다..!
    - 관련해서 iam 설정, github 처럼 외부 토큰을 사용해서 접근을 허가하기 위해서는 ec2, github 둘다에 iam 역할을 부여해줘야함.
- .github > workflow 밑에 cd 파일 등록하면 github에서 해당 이벤트가 일어날때마다 cd를 수행한다
- github actions로 배포를 할때, ssh 연결을 시도하려고하면 github actions 서버가(여기야말로 서버?!?) 내부에서 직접 ec2와 연결을 시도하고 거기서 cd 파일에 적은 명령어를 실행한다는 것도 신기했음.
